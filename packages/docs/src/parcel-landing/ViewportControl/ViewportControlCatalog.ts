import { Catalog } from '@composify/react/renderer';
import { ViewportControl } from './ViewportControl';

Catalog.register('ViewportControl', {
  component: ViewportControl,
  category: 'Landing',
  props: {},
});
