import { Catalog } from '@composify/react/renderer';
import { EditorControl } from './EditorControl';

Catalog.register('EditorControl', {
  component: EditorControl,
  category: 'Landing',
  props: {},
});
