---
layout: landing
title: Supercharge your editor
---

import { HeroBanner } from '../src/HeroBanner';
import { PlanGroup } from '../src/PlanGroup';
import { PriceCalculator } from '../src/PriceCalculator';
import { FeatureGroup } from '../src/FeatureGroup';
import { FeatureItemCompact } from '../src/FeatureItemCompact';

export default function Cloud() {
  return (
    <main>
      <HeroBanner
        tagline="Supercharge your editor"
        description="Hassle-free setup. Get the features that matter."
        actions={[
          { label: 'Start free ›', href: 'https://app.composify.net', primary: true },
          { label: 'Explore features →', href: '#features', primary: false },
        ]}
      />
      <PriceCalculator />
      <PlanGroup />
      <FeatureGroup
        id="features"
        title="The fastest way to build Server Driven UI."
        description="Build and ship with our cloud — everything you need is ready out of the box."
      >
        <FeatureItemCompact
          emoji="🌐"
          title="Managed hosting, unlimited bandwidth"
          description="We take care of deployment, scaling, and infrastructure — with unlimited bandwidth, so traffic spikes are never a concern."
        />
        <FeatureItemCompact
          emoji="🙌"
          title="Real-time collaboration"
          description="Work together in the same editor, see updates instantly, and avoid merge conflicts. Collaboration that actually feels seamless."
          planned={true}
        />
        <FeatureItemCompact
          emoji="⏳"
          title="Time-travel version history"
          description="Roll back to any previous version in seconds. Explore the full history and experiment freely without worrying about losing progress."
          planned={true}
        />
        <FeatureItemCompact
          emoji="💳"
          title="Start free and pay as you go"
          description="No upfront costs or hidden fees. Get started for free and pay only for the resources you use, scaling as your needs grow."
        />
      </FeatureGroup>
    </main>
  );
}
