import { Renderer } from '@composify/react/renderer';
import '../../../src/catalog';

export const source = `
  <FeatureItem
    title="Keep it simple with JSX"
    description="Use the markup you already know. Forget complex JSON or custom syntax."
    preview={<CodePreview asset="/assets/jsx" />}
    wide={false}
    spacing={{ top: 0, bottom: -50 }}
  />
`;

# Renderer

The **Renderer** takes a JSX string and renders it as a live UI, using components registered in your [Catalog](/docs/catalog).

:::code-group
<div data-title="Preview">
  <Renderer source={source} />
</div>

```tsx showLineNumbers [Code]
import { Renderer } from '@composify/react/renderer';

const source = `
  <FeatureItem
    title="Keep it simple with JSX"
    description="Use the markup you already know. Forget complex JSON or custom syntax."
    preview={<CodePreview asset="/assets/jsx" />}
    wide={false}
    spacing={{ top: 0, bottom: -50 }}
  />
`;

export function Example() {
  return <Renderer source={source} />;
}
```
:::

For a complete walkthrough on setting up the Catalog and integrating the full editor, please refer to the [Getting Started guide](/docs/getting-started).
